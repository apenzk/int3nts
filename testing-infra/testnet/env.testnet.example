# Intent Framework Testnet Deployment Keys
# Copy this file to .env.testnet and fill in your values
# NEVER commit .env.testnet to version control!
#
# WALLET IMPORT NOTES:
# - Movement keys (Nightly): Import via "Import Wallet" > paste private key (hex)
# - Base keys (MetaMask): Import via "Import Account" > paste private key (hex with 0x)
# - Solana keys (Phantom): Import via "Add/Connect Wallet" > "Import Private Key" > paste base58 key

# =============================================================================
# MOVEMENT BARDOCK TESTNET (Hub Chain) - Nightly Wallet Compatible
# =============================================================================

# Deployer - deploys the Move modules
# Generate with: aptos key generate --output-file movement-deployer.key
MOVEMENT_DEPLOYER_PRIVATE_KEY=
MOVEMENT_DEPLOYER_ADDR=

# Requester - creates intents (import into Nightly for browser access)
# Generate with: aptos key generate --output-file movement-requester.key
MOVEMENT_REQUESTER_PRIVATE_KEY=
MOVEMENT_REQUESTER_ADDR=

# Solver - fulfills intents (import into Nightly for browser access)
# Generate with: aptos key generate --output-file movement-solver.key
MOVEMENT_SOLVER_PRIVATE_KEY=
MOVEMENT_SOLVER_ADDR=

# Deployed intent module address (set automatically by deploy-to-movement-testnet.sh)
MOVEMENT_INTENT_MODULE_ADDR=

# Module admin private key (set automatically by deploy-to-movement-testnet.sh)
# Required by configure-movement-testnet.sh to set trusted remotes
MOVEMENT_MODULE_PRIVATE_KEY=

# =============================================================================
# BASE SEPOLIA (Connected Chain) - MetaMask Compatible
# =============================================================================

# Deployer - deploys IntentEscrow contract
# Generate with: cast wallet new
BASE_DEPLOYER_PRIVATE_KEY=
BASE_DEPLOYER_ADDR=

# Requester - creates escrows (import into MetaMask for browser access)
# Generate with: cast wallet new
BASE_REQUESTER_PRIVATE_KEY=
BASE_REQUESTER_ADDR=

# Solver - claims escrows (import into MetaMask for browser access)
# Generate with: cast wallet new
BASE_SOLVER_PRIVATE_KEY=
BASE_SOLVER_ADDR=

# Deployed contract addresses (set automatically by deploy-to-base-testnet.sh)
BASE_GMP_ENDPOINT_ADDR=
BASE_INFLOW_ESCROW_ADDR=
BASE_OUTFLOW_VALIDATOR_ADDR=
BASE_CHAIN_ID=84532

# =============================================================================
# SOLANA DEVNET (Connected Chain) - Phantom Wallet Compatible
# =============================================================================

# Deployer - deploys the SVM program
# Generate with: solana-keygen new
SOLANA_DEPLOYER_PRIVATE_KEY=
SOLANA_DEPLOYER_ADDR=

# Requester - creates escrows (import into Phantom for browser access)
# Generate with: solana-keygen new
SOLANA_REQUESTER_PRIVATE_KEY=
SOLANA_REQUESTER_ADDR=

# Solver - claims escrows (import into Phantom for browser access)
# Generate with: solana-keygen new
SOLANA_SOLVER_PRIVATE_KEY=
SOLANA_SOLVER_ADDR=

# USDC mint on Solana devnet (or your test token mint)
SOLANA_USDC_MINT=

# Deployed program IDs (set automatically by deploy-to-solana-devnet.sh)
SOLANA_PROGRAM_ID=
SOLANA_GMP_ID=
SOLANA_OUTFLOW_ID=
SVM_CHAIN_ID=4

# =============================================================================
# INTEGRATED-GMP KEYS
# =============================================================================

# Ed25519 keypair for signing approvals (base64 encoded)
# Generate with: cd integrated-gmp && cargo run --bin generate_keys
INTEGRATED_GMP_PRIVATE_KEY=
INTEGRATED_GMP_PUBLIC_KEY=

# EVM public key hash (keccak256 hash of ECDSA public key, last 20 bytes)
# This is the Ethereum address derived from the integrated-gmp's ECDSA public key (on-chain approver address)
# Generate with: cd integrated-gmp && INTEGRATED_GMP_CONFIG_PATH=config/integrated-gmp_testnet.toml cargo run --bin get_approver_eth_address
INTEGRATED_GMP_EVM_PUBKEY_HASH=


# =============================================================================
# OPTIONAL: API Keys for contract verification and RPC access
# =============================================================================

BASESCAN_API_KEY=

# Alchemy API key for Ethereum Sepolia RPC
# Get your free API key at: https://www.alchemy.com/
ALCHEMY_ETH_SEPOLIA_API_KEY=

# Alchemy API key for Base Sepolia RPC (optional - falls back to public RPC if not set)
# You can use the same key as Ethereum Sepolia if your Alchemy account supports it
# Get your free API key at: https://www.alchemy.com/
ALCHEMY_BASE_SEPOLIA_API_KEY=

# Solana RPC URL (optional - defaults to devnet public RPC)
# For production use Helius, QuickNode, or other providers
SOLANA_RPC_URL=https://api.devnet.solana.com

